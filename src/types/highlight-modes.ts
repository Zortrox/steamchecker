export const enum HighlightMode {
  BUNDLE = 'bundle',
  KEYS = 'keys', // partial_aliases = true; observer: '.unredeemed-keys-table'
  STORE = 'store',
  SEARCH = 'search', // observer: '.list-content'
  CHOICE = 'choice',
}
