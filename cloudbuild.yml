steps:
#update apt
- name: 'ubuntu'
  args: ["apt-get", "update"]

#create archive
- name: 'ubuntu'
  args: ["apt-get", "install", "zip"]

#create archive
- name: 'ubuntu'
  args: ["zip", "-r", "steamchecker_extension.zip", "extension"]